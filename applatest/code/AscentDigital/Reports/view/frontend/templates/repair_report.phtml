<?php
$itemsData = $block->getExchangeItemsData();
if($itemsData) {
?>

<form method="GET" action="">
     <div class="row">
        <div class="column-half" style="display: flex; float:right;">

            <button id="export_data" name="export_data" class="button-import" type="submit"> Export Data</button>

        </div>
    </div>
<table class="data table table-order-items history" id="my-orders-table">
      <caption class="table-caption"><?= $block->escapeHtml(__('Orders')) ?></caption>
      <thead>
          <tr>

              <th scope="col" class="col total"><?= $block->escapeHtml(__('Item Name')) ?></th>
              <th scope="col" class="col total"><?= $block->escapeHtml(__('Request Qty')) ?></th>
              <th scope="col" class="col total"><?= $block->escapeHtml(__('Condition')) ?></th>
              <th scope="col" class="col total"><?= $block->escapeHtml(__('Request Date')) ?></th>
              <th scope="col" class="col total"><?= $block->escapeHtml(__('Customer Name')) ?></th>
              <th scope="col" class="col total"><?= $block->escapeHtml(__('Reason')) ?></th>
              <th scope="col" class="col total"><?= $block->escapeHtml(__('Status')) ?></th>

          </tr>
      </thead>
      <tbody>
          <?php 

          foreach ($itemsData as $item) : 
            $itemStatus = $item['statusTitle'];
              if(!$item['statusTitle']) {
                  $itemStatus = '-';
              } ?>
              <tr>
                  <td class="col id"><?=$item['productName']?></td>
                  <td class="col id"><?=$item['request_qty']?></td>
                  <td class="col id"><?=$item['conditionTitle']?></td>
                  <td class="col id"><?=$item['created_at']?></td>
                  <td class="col id"><?=$item['customer_name']?></td>
                  <td class="col id"><?=$item['reason']?></td>
                  <td class="col id"><?=$itemStatus?></td>
              </tr>
          <?php endforeach; ?>
      </tbody>
  </table>
  </form>
  <?php } ?>